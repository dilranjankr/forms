Drop your install-guide screenshots here.

File naming  (PNG only):
  01.png   ->  Step 1  (Excel for the web open with a workbook)
  02.png   ->  Step 2  (Add-ins dropdown with 'More Add-ins' highlighted)
  03.png   ->  Step 3  (Office Add-ins dialog with 'Upload My Add-in' link highlighted)
  04.png   ->  Step 4  (Upload Add-in dialog showing Browse and Upload)
  05.png   ->  Step 5  (Home tab showing the new 'Kiser Invoice Tools' button)
  06.png   ->  Step 6  (Kiser Invoice Tools panel open, Invoice Form tab)

Then run:
  npm run guide:pdf

The PDF auto-embeds every PNG it finds (resized to 600px wide, aspect
preserved). Any step without a PNG falls back to a clean placeholder box.
